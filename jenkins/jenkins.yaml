jenkins:
  systemMessage: "accounting-service CI/CD (JCasC)"
  numExecutors: 2
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "admin"
  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

tool:
  jdk:
    installations:
      - name: "JDK25"
        home: "/opt/java/openjdk"

jobs:
  - script: |
      pipelineJob('accounting-service-pipeline') {
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/Nokhrin/accounting-service.git')
                }
                branch('*/dev')
              }
            }
            scriptPath('Jenkinsfile')
          }
        }
      }